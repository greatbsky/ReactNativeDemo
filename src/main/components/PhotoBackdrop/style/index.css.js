/**
 * my react native demo
 */

import {
  StyleSheet
} from 'react-native';

module.exports = StyleSheet.create({
    backdrop: {
        flex: 1,
        flexDirection: 'column',
        borderWidth: 2,
        borderColor: '#000'
      },
});
