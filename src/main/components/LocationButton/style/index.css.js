/**
 * my react native demo
 */

import {
  StyleSheet
} from 'react-native';

var baseFontSize = 16;

module.exports = StyleSheet.create({
    locationButton: {
        backgroundColor: '#FFDDFF',
        width: 200,
        padding: 25,
        borderRadius: 5
      },
});
