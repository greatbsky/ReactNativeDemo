/**
 * my react native demo
 */

import {
  StyleSheet
} from 'react-native';

module.exports = StyleSheet.create({
    bigText: {
      flex: 2,
      fontSize: 20,
      textAlign: 'center',
      margin: 10,
      color: '#000'
    }
});
